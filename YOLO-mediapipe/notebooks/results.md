# Results

_No benchmark results recorded yet._

Once you've trained a hand-detector checkpoint with `src/detection/train.py`
and evaluated it with `src/detection/evaluate.py`, record the key metrics
here (mAP50, mAP50-95, precision, recall) along with the training
configuration used, so results are comparable across runs.
